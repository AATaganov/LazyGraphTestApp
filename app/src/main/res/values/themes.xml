<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LazyInitialization" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="Theme.LazyInitialization" parent="Base.Theme.LazyInitialization" />

    <style name="ThemeButton" parent="TextAppearance.AppCompat.Widget.Button" >
        <item name="cornerRadius">5dp</item>
        <item name="android:minWidth">200dp</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>